export interface credentialsType {
    emailAdress: string;
    userName: string;
    password: string;
  }

  export interface isCorrectType{
      email: boolean,
      userName: boolean,
      password: boolean,
  }